<Window x:Class="CS2Migrate.App.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" Width="480" Background="Transparent"
        KeyDown="Window_KeyDown">
    <!-- Drawn rather than using a system dialog: MessageBox follows the Windows *system*
         theme, so it appears as a white box in front of a dark window. -->
    <Border Background="{DynamicResource LayerBackgroundBrush}"
            BorderBrush="{DynamicResource CardStrokeBrush}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="24,22,24,20">
                <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock x:Name="MessageText" FontSize="14" TextWrapping="Wrap" Margin="0,10,0,0"
                           LineHeight="20" Foreground="{DynamicResource TextSecondaryBrush}" />
            </StackPanel>

            <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}"
                    BorderBrush="{DynamicResource CardStrokeBrush}" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,7,7" Padding="24,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="SecondaryButton" Click="Secondary_Click" MinWidth="104"
                            Margin="0,0,8,0" Visibility="Collapsed" />
                    <Button x:Name="CancelButton" Click="Cancel_Click" MinWidth="104" Margin="0,0,8,0" />
                    <Button x:Name="PrimaryButton" Style="{StaticResource AccentButtonStyle}"
                            Click="Primary_Click" MinWidth="104" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
